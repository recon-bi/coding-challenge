# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build skiline-office-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t skiline-office-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system skiline-office-server && \
          adduser --system -G skiline-office-server skiline-office-server

COPY dist/skiline-office-server skiline-office-server
RUN chown -R skiline-office-server:skiline-office-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix skiline-office-server --omit=dev -f install

CMD [ "node", "skiline-office-server" ]
